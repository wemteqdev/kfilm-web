import React from 'react';
import SpecialVideos from '../SpecialVideos';

const RecentPage = (props) => (
        <SpecialVideos title="Recent" slug="recent" {...props} />
);

export default RecentPage;
